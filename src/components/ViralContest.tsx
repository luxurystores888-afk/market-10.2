import React from 'react';

const ViralContest = () => {
  return (
    <div>
      <h2>Share to Win 1000 Points!</h2>
      <button onClick={() => logReferralShare('contest')}>Share Now</button>
    </div>
  );
};

export default ViralContest;
